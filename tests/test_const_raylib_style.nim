## Test const block like in raylib example

const
  MaxBuildings = 100

  screenWidth = 800
  screenHeight = 450

echo("MaxBuildings = ", MaxBuildings)
echo("Screen: ", screenWidth, "x", screenHeight)
